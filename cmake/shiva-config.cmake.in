if( NOT SHIVA_FOUND )
  get_filename_component(SHIVA_CMAKE_CONFIG_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
  include(${CMAKE_INSTALL_PREFIX}/lib/cmake/shiva/shiva-targets.cmake)


  set(SHIVA_FOUND TRUE)

  # Export version number
  set( SHIVA_VERSION_MAJOR @SHIVA_VERSION_MAJOR@ )
  set( SHIVA_VERSION_MINOR @SHIVA_VERSION_MINOR@ )
  set( SHIVA_VERSION_PATCHLEVEL @SHIVA_VERSION_PATCHLEVEL@ )
endif()